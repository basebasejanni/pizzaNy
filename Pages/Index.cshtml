@page
@model PizzaTo.Pages.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Pizza[0].PriceID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Pizza) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PriceID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.PizzaID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.PizzaID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.PizzaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
